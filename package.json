{
  "name": "design-trace",
  "version": "0.1.0",
  "private": true,
  "description": "Local project timeline browser prototype.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js",
    "build:standalone": "node scripts/build-standalone.js",
    "build:single": "node scripts/build-standalone.js"
  },
  "engines": {
    "node": ">=18"
  }
}
